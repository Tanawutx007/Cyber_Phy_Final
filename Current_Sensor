/*
  ACS712 Current Sensor Calibration & Measurement
  ใช้ได้กับ ACS712 รุ่น 5A / 20A / 30A
  เขียนโดย ChatGPT 💡
*/

#if defined(ESP32)
  #define CURRENT_PIN 27   // ADC ขา 34 (ESP32)
  const float ADC_REF = 3.3;    // ESP32 ใช้ 3.3V
  const int ADC_MAX = 4095;     // 12-bit ADC
#else
  #define CURRENT_PIN A0    // ขา A0 (Arduino UNO)
  const float ADC_REF = 5.0;    // UNO ใช้ 5V
  const int ADC_MAX = 1023;     // 10-bit ADC
#endif

// 🔹 ปรับตามรุ่นที่ใช้
const float SENSITIVITY = 0.185;   // 0.185 V/A → รุ่น 5A
// const float SENSITIVITY = 0.100; // รุ่น 20A
// const float SENSITIVITY = 0.066; // รุ่น 30A

float zeroOffset = 0;  // ค่าศูนย์เริ่มต้น (จะหาตอนคาลิเบรต)

void setup() {
  Serial.begin(115200);
  delay(1000);
  Serial.println("\n=== ACS712 Current Sensor Calibration ===");
  Serial.println("1️⃣ ถอดโหลดออก (ไม่ให้มีกระแส)");
  Serial.println("2️⃣ พิมพ์คำว่า 'cal' แล้ว Enter เพื่อคาลิเบรตศูนย์");
  Serial.println("3️⃣ พิมพ์คำว่า 'run' เพื่อเริ่มวัดกระแสจริง");
}

void loop() {
  if (Serial.available()) {
    String cmd = Serial.readStringUntil('\n');
    cmd.trim();

    if (cmd == "cal") {
      zeroOffset = calibrateZero();
      Serial.print("✅ Zero Offset (V) = ");
      Serial.println(zeroOffset, 4);
      Serial.println("พิมพ์ 'run' เพื่อเริ่มวัดกระแส");
    }

    else if (cmd == "run") {
      Serial.println("🔌 เริ่มวัดกระแสจริง (Ctrl+C เพื่อหยุด)");
      while (true) {
        float current = readCurrent();
        Serial.printf("Current: %.3f A\n", current);
        delay(500);
      }
    }
  }
}

// -------------------- ฟังก์ชันคาลิเบรตศูนย์ --------------------
float calibrateZero() {
  Serial.println("🌀 กำลังคาลิเบรต... (อ่านค่าเฉลี่ย 1000 ครั้ง)");
  long sum = 0;
  for (int i = 0; i < 1000; i++) {
    sum += analogRead(CURRENT_PIN);
    delay(2);
  }
  float avg = sum / 1000.0;
  float Vout = (avg / ADC_MAX) * ADC_REF;
  return Vout;
}

// -------------------- ฟังก์ชันวัดกระแส --------------------
float readCurrent() {
  const int N = 50;  // เฉลี่ยค่า 50 ครั้ง
  long sum = 0;
  for (int i = 0; i < N; i++) {
    sum += analogRead(CURRENT_PIN);
    delay(2);
  }
  float avg = sum / (float)N;
  float Vout = (avg / ADC_MAX) * ADC_REF;

  // หาค่ากระแสจริง (บวกหรือลบ)
  float I = (Vout - zeroOffset) / SENSITIVITY;
  return I;
}
